class Solution{
    static int inSequence(int A, int B, int C){
        // code here
         if(B==A)
            return 1;
        
        if((A>B && C>0) || (A<B && C<0) || C==0)
            return 0;
        
        if(((B-A)%C)==0)
            return 1;
        
        return 0;
    }
}
